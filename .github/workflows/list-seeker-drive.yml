name: List Seeker Drive Assets

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  list:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary workflow disabled after manifest retrieval."
